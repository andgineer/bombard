supply:  # you can redefine it from command line (--supply)
  host: https://serpapi.com/search
prepare:  # Get links from search results.
  search:
    url: "{host}&q=Coffee"
    method: GET
    headers:
      Content-Type: application/json
    script: |
      for link in resp[organic_results.sitelinks.inline.]['link']:
        reload(ammo.visitLink, link=link)
ammo:
  visitLink:
    url: "{base}users"
    method: GET
